To make a complete sqlite3 version of the tables from existing Sybase versions, do:

$ ./make_new_tables.py --tstart=1999:001 --cmds --cmd-states --tl-processing # takes a few minutes
$ cp db_base.db3 $SKA/data/cmd_states/cmd_states.db3  # as "aca" user

